#pragma once

#if !_SERVER

enum class EHorizontalAlignment
{
	Left,
	Center,
	Right
};

enum class EVerticalAlignment
{
	Top,
	Center,
	Bottom
};

enum class EAlignmentType
{
	Vertical,
	Horizontal
};

#endif